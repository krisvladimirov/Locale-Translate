using System;
namespace LocalisationTranslator
{
    /// <summary>
    /// 
    /// </summary>
    public class Program
    {
        public static int Main(string[] args)
        {
            App.Process();

            // Console.Write("Ae bai hui");
            // var s = new string('\b', 10);
            // Console.Write(s);
            // Console.Write("Ae urud smotan");
            //outcome = Program.ReadLocalisation();
            //if (outcome)
            //{
            //    Console.WriteLine("Successful");
            //}
            //else
            //{
            //    Console.WriteLine("Unsuccessful");
            //    return -1;
            //}


            // foreach(var expando in records)
            // {
            //     var record = (IDictionary<string, object>) expando;
            //     var sb = new StringBuilder("");
            //     foreach (var kvp in record)
            //     {
            //         sb.Append($"{kvp.Value} ");
            //     }
            //     sb.Append("\n");
            //     Console.WriteLine($"{sb}");
            // }


            // var x = new List<int>(){5, 6, 9};
            // var y = new List<int>();
            // Console.WriteLine(y.First());
            // var ind = x.FindIndex(x => x >= 0 + 2);
            // Console.WriteLine(ind);

            

            // if (errors.Count > 0)
            // {
            //     Program.PrintLog();
            // }
            // var c = 0;
            // var resetC = 0;
            // using (var progress = new ProgressBar(100)) {
            //     for (int i = 0; i <= 100; i++) {
            //         progress.Report(i);
            //         Thread.Sleep(25);
            //     }
            //     c = progress.count;
            //     resetC = progress.resetCounter;
            // }
		    // Console.WriteLine("Done.");
            // Console.WriteLine(c);
            // Console.WriteLine(resetC);

            // Console.WriteLine(!Regex.IsMatch("A little pocket sized man with", "^((?!\\<(|\\/)[a-z][a-z0-9]*>).)*$"));

            return 0;
        }
    }
}
